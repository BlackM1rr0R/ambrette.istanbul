import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.perfumenewwebsite',
  appName: 'ambrette-perfume',
  webDir: 'build'
};

export default config;
